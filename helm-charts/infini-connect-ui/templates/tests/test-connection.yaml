apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infini-connect-ui.fullname" . }}-test-connection"
  labels:
    {{- include "infini-connect-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infini-connect-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
